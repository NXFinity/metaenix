/**
 * Resource Type Enum
 * 
 * Defines the types of resources that can be tracked
 */
export enum ResourceType {
  PROFILE = 'profile',
  POST = 'post',
  VIDEO = 'video',
  // Add more resource types as needed
}

